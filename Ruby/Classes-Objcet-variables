class BookInStock
  # constructor
  def initialize(isbn, price)
    @isbn = isbn
    @price = Float(price)
  end

  #method ovverriding
  def to_s
    "ISBN: #{@isbn}, price: #{@price}"
  end

end

b1 = BookInStock.new("isbn1", 3)
p b1 #print - ISBN: isbn1,price: 120.0
# state is private to those objects no other object's can access an object's instance variables

#instance attribute access
 -  using method
  e.g -
    def isbn
      @isbn
    end
 - Ruby provides a convenient shortcut. attr_reader creates these attribute reader methods
  e.g
    class BookInStock
     attr_reader :isbn, :price
    end

    puts book.isbn
  - assign value to the variable
  e.g
    def isbn=(new_isbn)
      @isbn = new_isbn
    end

  #Ruby provides a shortcut for creating these simple attribute-setting methods
   - attr_reader
   - attr_writer
   - attr_accessor # both a reader and a writer for a given attribute

  Virtual Attributes
  -------------------
